# Oliver Meihls
# Used terminal to create a new vim file, then entered whatever text I was told to.
# I had to lookup the commands for saving and quitting, and quitting without saving.
# Also had to lookup the commands for copying and renaming the file.
# I'm now editing the file and I'm not sure if I'm doing it correctly but we will see.

syntax on
set ruler
set autoindent
set number
set ignorecase
color elflord



